<%= form_for(@department_staff) do |f| %>
  <% if @department_staff.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@department_staff.errors.count, "error") %> prohibited this department_staff from being saved:</h2>

      <ul>
      <% @department_staff.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :department %><br />
    <%= f.text_field :department %>
  </div>
  <div class="field">
    <%= f.label :staff %><br />
    <%= f.text_field :staff %>
  </div>
  <div class="field">
    <%= f.label :designation %><br />
    <%= f.text_field :designation %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= form_for(@subject_category) do |f| %>
  <% if @subject_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@subject_category.errors.count, "error") %> prohibited this subject_category from being saved:</h2>

      <ul>
      <% @subject_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
